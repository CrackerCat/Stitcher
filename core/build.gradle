archivesBaseName = "stitcher-core"

repositories {
    mavenCentral()
}

dependencies {
    compile "org.ow2.asm:asm-debug-all:5.2"
    compile "org.apache.logging.log4j:log4j-api:2.8.1"
    compile "com.eclipsesource.minimal-json:minimal-json:0.9.5"
}